node_modules/
.jig/
.env
.env.*
!.env.example
jig.db*
*.pem
*.key
*.p12
*.pfx
credentials/
secrets/
.railway/
# .agents/skills is the cross-agent skills directory (Claude Code, Codex,
# Cursor, OpenCode all read it), so it is source and must be committed.
# .codex/ and .claude/ are per-tool local state and stay ignored.
.codex/
.a5c/
.worktrees/
jig_debug.log
.next
src/config/deploy-defaults.generated.json

# Authored jigs are per-instance runtime state; the store (SQLite) is
# authoritative and jigs/ is only a legacy import source.
jigs/
runtime/
check/
jig.log
.claude/
tmp/
test/.tmp/
