{
  "name": "jig",
  "version": "0.1.126",
  "description": "AI workflow automation — deterministic execution with LLM hatches",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "keywords": ["ai-agent", "autonomous-agent", "mcp", "workflow-automation", "llm", "deterministic-execution"],
  "scripts": {
    "jig": "bun run src/cli.ts",
    "start": "bun run src/cli.ts start",
    "typegen": "bun run src/mcp/typegen.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "jig": "src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/diff": "^8.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "croner": "^10.0.1",
    "diff": "^9.0.0",
    "open": "^11.0.0",
    "openai": "^6.32.0"
  }
}
